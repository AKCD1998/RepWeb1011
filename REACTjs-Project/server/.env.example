# Profile: render-live
# In deployed environments, DATABASE_URL should point at the canonical Render PostgreSQL database.
# For local dev/testing, use the local-simulation profile in server/.env.local-simulation instead of editing this file.
DATABASE_URL=
PORT=5050
CORS_ORIGIN=http://localhost:5173,https://your-frontend-host.example
JWT_SECRET=change-me
